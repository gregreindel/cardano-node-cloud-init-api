{
    "name": "cardano-node-generator-api-endpoints",
    "description": "Cardano node generator API",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "shelljs": "^0.8.4"
    },
    "devDependencies": {
        "jest": "^25.4.0"
    },
    "scripts": {
        "test": "eval $(cat .env)  jest",
        "local": "eval $(cat .env) node local.js"
    }
}
